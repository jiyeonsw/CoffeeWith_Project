<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.data.dao.DbInsertDaoDao">
	<insert id="insertCf" parameterType="didto">
		insert into cf values (null, #{cf_id},#{cf_tel},#{open_time},#{off_day},#{loc_addr},#{loc_sigu},#{loc_x},#{loc_y},0,0,#{menu},#{cf_txt})
	</insert>
</mapper>